/**
 * AUTO-GENERATED FILE — DO NOT EDIT BY HAND.
 *
 * This is a placeholder committed before the first `supabase gen types`
 * run so the repo has a stable import path (`src/types/database.ts`) from
 * day one. Replace it immediately after running M7.1's migrations:
 *
 *   npm run gen-types
 *
 * Then commit the regenerated file. CI (.github/workflows/supabase-schema-check.yml)
 * will fail any PR where this file drifts from the live schema.
 */

export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
